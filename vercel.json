{
  "installCommand": "corepack enable pnpm && pnpm install --ignore-scripts --no-frozen-lockfile --no-optional",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
