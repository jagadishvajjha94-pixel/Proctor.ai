# Avoid install failures on Vercel (e.g. exit 236)
# pnpm (used when installCommand runs pnpm):
strict-peer-dependencies=false
auto-install-peers=true
# npm ignores the above; use: npm install --legacy-peer-deps if switching to npm on Vercel
